rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow writes (uploads) for any authenticated user
    // You could further restrict this to specific user roles (e.g., admin) if needed
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
